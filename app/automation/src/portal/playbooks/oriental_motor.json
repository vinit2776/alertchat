{
  "portal_id": "oriental_motor",
  "name": "Oriental Insurance Company Limited — Motor",
  "base_url": "https://orientalinsurance.org.in",
  "insurance_type": "motor",

  "_note": "Login is a modal dialog triggered from the homepage header (#login-btn.header-login-btn). The promo popup (p-dialog) must be dismissed first. Captcha is a canvas-rendered text image. No OTP — just username + password + text captcha. Selectors verified via DOM inspection 2026-06-05.",

  "login": {
    "url": "/",
    "trigger_selector": "#login-btn.header-login-btn",
    "username_field": "#username",
    "password_field": "#password",
    "submit": "#continue-btn",
    "success_indicator": ".user-profile-name, .agent-name, #logout-btn, .logout, [class*='user-info'], [class*='agent-dashboard'], .profile-menu, .welcome-user",
    "dismiss_modals": [
      "button[aria-label='Close']"
    ],
    "captcha": {
      "image_selector": "canvas.captcha-canvas",
      "input_selector": "#captcha",
      "refresh_selector": ".btn.reset-btn-icon"
    }
  },

  "quote_flow": [],

  "_status": "login_only — quote_flow to be mapped after live browser session capture"
}
